# torch/torchvision/torchaudio come from the system pyenv install (see .venv --system-site-packages);
# do not pin them here or uv will try to redownload the CUDA build.
anthropic>=0.40.0
pydantic>=2.0
numpy<2.0
scipy
librosa
soundfile
Pillow
opencv-python
imageio
imageio-ffmpeg
tqdm
pyyaml
